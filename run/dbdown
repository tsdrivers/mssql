#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

echo "Stopping MSSQL service..."
docker compose -f "$PROJECT_ROOT/docker-compose.yml" down

echo "MSSQL service stopped."
