# mssql-ts-ffi test configuration
# Copy this file to .env to override defaults.

# Host port for Docker MSSQL container (container always listens on 1433 internally).
# Default: 14330
# MSSQL_HOST_PORT=14330

# SA password for the Docker MSSQL container.
# Default: DevPassword1!
# MSSQL_SA_PASSWORD=DevPassword1!

# SA-level connection string for database setup (connects to master).
# Default is derived from MSSQL_HOST_PORT and MSSQL_SA_PASSWORD above.
# Override for external instances like Windows SQL Express:
# MSSQL_SA_CONNECTION=Server=localhost\SQLEXPRESS;Database=master;Integrated Security=true;TrustServerCertificate=true;

# Set to 1 to skip Docker clean/dbup (e.g. when using external SQL Express).
# MSSQL_SKIP_DOCKER=1

# Set to 1 to skip Rust build (reuse existing .bin/ artifacts).
# MSSQL_SKIP_BUILD=1
